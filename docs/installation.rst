============
Installation
============

At the command line::

    $ easy_install stochastic_approximation

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv stochastic_approximation
    $ pip install stochastic_approximation